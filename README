TODO(ipeet): Keep this up to date.  (Who am I kidding?)

This is currently a pretty simple project.  It currently depends
on qt4 to do the actual display (and I may build a real GUI sometime),
so you will need libqt4-dev to build.

Other than that, it should be a simple matter of running make.
